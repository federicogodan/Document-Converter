<%= form_for(@webhoook) do |f| %>
  <% if @webhoook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webhoook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
      <% @webhoook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br />
    <%= f.check_box :deleted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
  <% if not @webhoook.nil?%>
  <% #render webhoook_whsents_path %>
  <% end %>
<% end %>
